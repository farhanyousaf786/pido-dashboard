import React from 'react';

function AdminSettings() {
  return (
    <section className="page-shell">
      <h1 className="page-shell__title">Admin Settings</h1>
      <p className="page-shell__subtitle">
        Manage admin profile and permissions (content to be added).
      </p>
      <div className="page-shell__empty">
        This admin settings page is intentionally empty for now.
      </div>
    </section>
  );
}

export default AdminSettings;
