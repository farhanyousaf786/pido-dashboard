import React from 'react';

function Settings() {
  return (
    <section className="page-shell">
      <h1 className="page-shell__title">Settings</h1>
      <p className="page-shell__subtitle">
        Application preferences and configuration (content to be added).
      </p>
      <div className="page-shell__empty">
        This settings page is intentionally empty for now.
      </div>
    </section>
  );
}

export default Settings;
